/**
 * Widget documentation generator export
 * This ensures compatibility with the section generation system
 */

// Export a no-op function since we're using the master generator approach
export default async function generateWidgetDocumentation() {
    console.log('✅ Widget documentation handled by master generator');
    // Do nothing - the master generator handles widget docs
    return;
}
