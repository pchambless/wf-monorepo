import { createSelectWidget } from './createSelectWidget.jsx';

/**
 * Product Type selector widget
 * Uses prodTypeList eventType for data source
 */
const SelProdType = createSelectWidget('prodTypeList');

export default SelProdType;
