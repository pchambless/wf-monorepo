import { createSelectWidget } from './createSelectWidget.jsx';

/**
 * Product selector widget
 * Uses prodList eventType for data source
 */
const SelProd = createSelectWidget('prodList');

export default SelProd;
