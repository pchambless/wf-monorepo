import React from 'react';
import { observer } from 'mobx-react-lite';

const IngredientBatches = observer(() => {
  return <div>Ingredient Batches (Stub)</div>;
});

export default IngredientBatches;
