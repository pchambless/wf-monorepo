export const pageTemplate = {
  category: "page",

  // Cards to show in Component Detail tab for page eventTypes
  detailCards: [
    "basics",           // Basic Properties (category, title, cluster, purpose)
    "pageRouting",      // Route paths, parameters, guards (future)
    "componentLayout",  // Child components array (page layout)
    "workflowTriggers",  // Workflow triggers (onLoad, onRouteChange, etc.)
    "workflows"         // Page-level workflows (onLoad, onRouteChange, etc.)
  ]
};

