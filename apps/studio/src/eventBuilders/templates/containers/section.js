export const sectionTemplate = {
  category: "section",

  // Cards to show in Component Detail tab for section eventTypes
  detailCards: [
    "basics",           // Basic Properties (category, title, cluster, purpose)
    "sectionLayout",    // Header, collapsible, bordered, padding, variant
    "componentLayout"   // Child components array (section content)
  ]
};