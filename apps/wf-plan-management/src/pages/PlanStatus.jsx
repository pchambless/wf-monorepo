import React from "react";
import SimplePage from "../components/SimplePage";

const PlanStatus = () => {
  return (
    <SimplePage
      title="Plan Status"
      description="This page will show the status filtering widget and filtered plan lists."
      note="Phase 3 will integrate with select-PlanStatus eventType for real filtering."
    />
  );
};

export default PlanStatus;
