import React from "react";
import SimplePage from "../components/SimplePage";

const CreatePlan = () => {
  return (
    <SimplePage
      title="Create New Plan"
      description="This page will show the plan creation form."
      note="Phase 3 will integrate with form-planDetail eventType for plan creation."
    />
  );
};

export default CreatePlan;
