export const tabsTemplate = {
  category: "tabs",

  // Cards to show in Component Detail tab for tabs eventTypes  
  detailCards: [
    "basics",           // Basic Properties (category, title, cluster, purpose)
    "tabLayout",        // Orientation, defaultActive, variant, closeable
    "componentLayout"   // Child components array (individual tabs)
  ]
};