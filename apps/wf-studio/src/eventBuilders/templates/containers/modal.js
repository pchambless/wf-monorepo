export const modalTemplate = {
  category: "modal",

  // Cards to show in Component Detail tab for modal eventTypes
  detailCards: [
    "basics",           // Basic Properties (category, title, cluster, purpose)
    "modalLayout",      // Size, backdrop, dismissible, centered, scrollable
    "componentLayout"   // Child components array (modal content)
  ]
};