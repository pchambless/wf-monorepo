export const buttonTemplate = {
  category: "button",

  // Cards to show in Component Detail tab for button eventTypes  
  detailCards: [
    "basics",           // Basic Properties (category, title, cluster, purpose)
    "button",      // Button-specific properties (label, variant, size, etc.)
    "workflowTriggers"  // onClick workflows
  ]
};